#include <stdio.h>
int index;
int arr[1024]
int main()
{

    printf("1--添加敏感字符");
    printf("2--删除最后一个敏感字符");
    printf("3--显示所有敏感字符");
    printf("4--替换敏感字符");
    printf("5--退出");

     printf("请输入相应的功能编号:");
     int code;
     scanf("%d",&code);
     
     if (code == 1) {
         
         
     }
      if (code == 2) {
         /* code */
     }
      if (code == 3) {
         /* code */
     }
      if (code == 4) {
         /* code */
     }
      if (code == 5) {
         /* code */
     }
     


    return 0;
}